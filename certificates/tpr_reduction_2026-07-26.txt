# TPR (計画20) 段階1 内部還元監査 -- generated 2026-07-26T06:50:18Z
# script: scripts/tpr_thermo_audit.py  seed 20260728  sympy 1.14.0
# 判定に浮動小数点を使用していない（表示のみ sp.N）
# 手順: docs/tpr-kill-plan.md §2 段階1
TPR（計画20）段階1 内部還元監査 — W1-W4
seed 20260728 / sympy 1.14.0 / 判定に浮動小数点を使用しない

==========================================================================
W1a  減衰床の閉形式と凍結文書の再現（規約の同定）
==========================================================================
  prop:phase-n  D/Gamma = diag(1/2, 13/20, 17/20, 21/20)
                凍結値   = diag(1/2, 13/20, 17/20, 21/20)      一致=True
  prop:phase-h  D/Gamma = diag(0, 0, 1/2)
                凍結値   = diag(0, 0, 1/2)      一致=True
  閉形式: Lambda_j = ( Gamma_out(j) + Gamma_out(1) ) / 2
  -> W1a PASS   [両方の凍結 D を厳密再現]

==========================================================================
W1b  有限温度の核持ち上げ: D(t) = D_0 + delta(t)*I（D_L = I）
==========================================================================
  D(t)/Gamma      = diag(t/2, t/2, t/2 + 1/2)
  D(t)/Gamma - D_0 = diag(t/2, t/2, t/2)
  delta(t)        = t/2   （= Gamma_out(1)/(2*Gamma)）
  持ち上げが恒等行列に比例: True
  dim Ker D (t>0) = 0   -> P_full = 0 -> R_(S,0) = 0 -> nu >= 1
  機構の同定: 熱的持ち上げは ガイド §4.2 赤線7（fixed kernel lifting）。
              提案 §3 T1(a) の「核次元の勘定」ではない。
              持ち上げ行列 D_L = I は提案24 LKCT の族の特別な切片。
  -> W1b PASS   [D_L = I, delta(t) = t/2 > 0]

==========================================================================
W1c  [負制御] t = 0（ゼロ温度）で保護核が戻ること
==========================================================================
  D(0)/Gamma = diag(0, 0, 1/2)   dim Ker D = 2
  負制御が期待どおり: 温度を切ると保護核が復活する（検定力あり）
  -> W1c PASS   [dim Ker D = 2]

==========================================================================
W1d  斜交 Pi_S による反例: 有限温度でも P_cut != 0 が起こりうる
==========================================================================
  D = diag(1/10, 3/5)   （有限温度 t = 1/5、正定値）
  u = [1, 1]   w = D u = [1/10, 3/5]   s^T u = -1/2
  Pi = [[6/5, -1/5], [6/5, -1/5]]
  Pi^2 = Pi : True      Pi D u = [0, 0]
  D_cut = Pi D Pi|_(Ran Pi) は特異: True
  => P_cut != 0。eq:R0 の cut 項が生き残り R_(S,0) != 0（Class III）。
     すなわち T1 は仮定 (E3)（切断クラスの対称性制限）なしには偽。
     凍結 def:operational-sector は 'the Riesz splitting may be oblique' と明記している。
     ただし rem:gksl-model により、この Pi を与える G_S が GKSL admissible かは模型ごとの監査事項。
  -> W1d PASS   [斜交圧縮が特異]

==========================================================================
W1e  [負制御] 直交 Pi_S では W1d の反例が消えること
==========================================================================
  Pi(orth) = [[1/2, 1/2], [1/2, 1/2]]   Pi^2=Pi: True
  圧縮の固有値 = u^T D u / u^T u = 7/20  > 0 : True
  負制御が期待どおり: 正定値 D の直交圧縮は常に可逆。斜交性が本質。
  -> W1e PASS   [直交圧縮は可逆]

==========================================================================
W2a  熱融解端（Gamma*eps ~ 1）と LKCT 稜線（Gamma^2*eps ~ 1）の分離
==========================================================================
  Schur 補元の大 Gamma 展開: S - b = (Gamma**2*epsilon**2 + Gamma**2*epsilon - f*h)/(Gamma*(epsilon + 1))
  融解端   Gamma_melt  = b/epsilon      -> eps^(-1)
  LKCT稜線 Gamma_ridge = sqrt(f)*sqrt(h)/sqrt(epsilon)   -> eps^(-1/2)
  eps 冪: melt = -1   ridge = -1/2
  => 2つは別スケール。提案の Gamma*(beta) ~ exp(beta*Delta) は融解端であり、
     LKCT/RISEI の eps^(-1/2) 稜線とは一致しない（重複ではない）。
     ただし融解端そのものは ガイド §4.2 赤線7 が名指しで扱う有限窓現象。
  -> W2a PASS   [eps^-1 vs eps^-1/2]

==========================================================================
W3a  T2 の仮定 (P2) が ass:singular と矛盾すること
==========================================================================
  D = diag(0,0,1/2)   P = diag(1,1,0)
  P*D = 0 : True      D*P = 0 : True

  eq:R0 より R_(S,0) は P で挟まれた量である:
      R_(S,0) = p^dag P B_P^-1 P c  -  (cut 側の同型項)
  ゆえに R_(S,0) を運ぶ成分は Ran P = Ker D の中にあり、
  **定義により Gamma スケールで減衰しない**。

  一方、提案 §3 T2 の仮定 (P2) と証明経路は
      「R_(S,0) != 0 は Gamma スケール散逸子の固有基底に対し非対角な
        コヒーレンス c_perp を要求する」
      「Gamma スケール位相緩和は c_perp を速度 ~Gamma|c_perp|^2 で破壊し、
        定常性より同速度の再生成電流が要る」
  と述べる。前者は Ran P の外に、後者は Gamma 減衰を受ける成分に、
  それぞれ R_(S,0) の担い手を置いている。**両立しない。**

  => T2 の証明経路は設定と矛盾する。下界 sigma >= C*Gamma*c_0^2 の
     導出根拠が存在しない。T2 は Conditional ですらなく Conjecture。
  -> W3a PASS   [Ran P = Ker D、(P2) と両立しない]

==========================================================================
W4a  sigma_bath は N^2 単項式台 / sigma_cut のみが log を出す
==========================================================================
  浴のアフィニティ A_bath = ln(1/3) — Gamma 依存: False
  切断のアフィニティ A_cut = q*log(Gamma) + log(kappa_0)
    ln(Gamma) を含む: True

  => sigma = sigma_bath + sigma_cut と分離すれば、
     sigma_bath は ass:bivariate を満たし thm:polyhedral-selection を継承できる。
     log 補正は sigma_cut にのみ現れ、これは T7（介入の実装コスト）の対象。
  提案 §3 T4 の「mu は log 補正つき」という一括りの定義は分離すべきであり、
  §6 Stop 条件4（log 補正が leading power を不定にする）は分離により回避できる。
  -> W4a PASS   [分離すれば継承可能]

==========================================================================
W4b  単一温度 sigma == 0 / 二温度 sigma ∝ Gamma（mu in {0,1} の起源）
==========================================================================
  単一温度 (t1 = t2 = t):  sigma = 0
  二温度 (t1=1/2, t2=1/5): sigma = Gamma * (-log(2)/9 + log(5)/9)
    係数は Gamma 非依存: True    厳密に正: True
    （参考表示のみ、判定に不使用）数値 = 0.1018100813

  => mu = 0 <-> Gamma スケール浴が詳細釣合い（単一温度）
     mu = 1 <-> Gamma スケール浴が非平衡（複数温度）
  アフィニティ ln(t_nu/r) は Gamma 非依存の定数なので、
  **浴の EP は Gamma について log 補正を持たない**。
  -> W4b PASS   [sigma_single == 0, sigma_two ∝ Gamma]

==========================================================================
判定一覧
==========================================================================
  W1a   PASS
  W1b   PASS
  W1c   PASS
  W1d   PASS
  W1e   PASS
  W2a   PASS
  W3a   PASS
  W4a   PASS
  W4b   PASS

  全ゲート PASS
